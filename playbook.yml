---

- name: Install required packages
  become_user: root
  gather_facts: false
  hosts: localhost
  tasks:
    - name: Install Git
      become_user: root
      ansible.builtin.package:
        name: git
        state: present
    - name: Install Python3
      ansible.builtin.package:
        name: python3
        state: present