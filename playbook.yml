---

- name: Install required packages
  become_user: root
  gather_facts: false
  hosts: localhost
  tasks:
    - name: Install Git
      become_user: root
      ansible.builtin.apt:
        name: git
        state: present
    - name: Install Python3
      ansible.builtin.apt:
        name: python3
        state: present